{
	"name": "@gaber32188/styles",
	"version": "0.2.0",
	"style": "./dist/index.css",
	"sideEffects": [
		"*.css"
	],
	"license": "MIT",
	"files": [
		"dist/**"
	],
	"scripts": {
		"build": "parcel build index.css",
		"dev": "parcel watch index.css --port $(../../scripts/getPort.sh)",
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
	},
	"devDependencies": {
		"parcel": "^2.10.3"
	},
	"publishConfig": {
		"access": "public"
	}
}
